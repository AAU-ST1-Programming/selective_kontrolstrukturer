/* Question 1a*/
#include <stdio.h>
int main()
{
    int a = -1;
    printf("\nWhen value of a is: %d", a);
    if (a)
        printf(", it evaluates to TRUE\n");
    else
        printf(", it evaluates to FALSE\n");
}
